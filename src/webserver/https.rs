use rustls::ServerConfig;
/* 
pub(super) fn build_ssl_config() -> anyhow::Result<ServerConfig> {
    let conf = ServerConfig::builder()
        .with_safe_defaults()
        .with_no_client_auth()
        .with_single_cert(certs, private_key)?;
    Ok(conf)
}
*/