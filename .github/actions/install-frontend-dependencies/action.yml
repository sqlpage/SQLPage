name: Install and build the frontend
description: Set up Node.js, install the frontend workspace dependencies and bundle the browser assets
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        node-version: 26
        cache: npm
    - run: npm ci --ignore-scripts
      shell: bash
    - run: npm run build
      shell: bash
