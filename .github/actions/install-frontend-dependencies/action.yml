name: Install frontend dependencies
description: Set up Node.js and install the frontend workspace dependencies
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v7
      with:
        node-version: 26
        cache: npm
    - run: npm ci --ignore-scripts
      shell: bash
